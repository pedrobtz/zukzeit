% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forecast.R
\name{forecast.zuk_model}
\alias{forecast.zuk_model}
\title{Forecast future values with a foundation model}
\usage{
\method{forecast}{zuk_model}(
  object,
  new_data,
  h = 1L,
  quantile_levels = c(0.1, 0.5, 0.9),
  index = NULL,
  key = NULL,
  target = NULL,
  batch_size = NULL,
  device = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{zuk_model}.}

\item{new_data}{A \code{tsibble} (index/key inferred) or a \code{data.frame}.}

\item{h}{Integer forecast horizon.}

\item{quantile_levels}{Numeric vector of quantile levels in \verb{(0, 1)}.}

\item{index, key, target}{Column names; required for the \code{data.frame} method,
inferred for a \code{tsibble}.}

\item{batch_size, device}{Passed to \code{\link[=zuk_run_batches]{zuk_run_batches()}}.}

\item{...}{Unused.}
}
\value{
A \code{zuk_forecast}.
}
\description{
The convenience surface: hand a panel of history and a horizon, get a
\code{zuk_forecast} back. This is a method for \code{\link[generics:forecast]{generics::forecast()}}, the same
generic fabletools and modeltime dispatch on, so attaching zukzeit alongside
them never masks the verb.
}
\examples{
model <- zuk_pretrained("stub")

# A plain data frame needs its index and target columns named.
history <- data.frame(day = 1:60, sales = cumsum(rep(2, 60)) + 100)
fc <- forecast(model, history, h = 5, index = "day", target = "sales")
fc

as.data.frame(fc)

# A panel: one row per series and time step, keyed by `store`.
panel <- data.frame(
  store = rep(c("a", "b"), each = 60),
  day   = rep(1:60, 2),
  sales = c(cumsum(rep(2, 60)) + 100, cumsum(rep(1, 60)) + 50)
)
forecast(model, panel, h = 3, index = "day", key = "store", target = "sales")

zuk_unload("stub")
}
