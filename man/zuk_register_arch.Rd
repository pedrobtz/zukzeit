% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{zuk_register_arch}
\alias{zuk_register_arch}
\alias{zuk_registry_has}
\alias{zuk_registry_archs}
\title{Register a model architecture}
\usage{
zuk_register_arch(architecture, constructor, overwrite = FALSE)

zuk_registry_has(architecture)

zuk_registry_archs()
}
\arguments{
\item{architecture}{Character scalar key (matched against \code{config.json}).}

\item{constructor}{A function \verb{function(config, weights)}.}

\item{overwrite}{Logical; if \code{FALSE} (default) re-registering an existing key
errors.}
}
\value{
\code{zuk_register_arch()} invisibly returns the architecture key;
\code{zuk_registry_has()} returns one logical value; and
\code{zuk_registry_archs()} returns the registered keys as a character vector.
}
\description{
Associates an architecture key with a constructor. The constructor must have
the signature \verb{function(config, weights)} and return a \code{\link[=new_zuk_model]{new_zuk_model()}}.
}
\details{
Registration changes only the current R session's constructor mapping. It
does not add a checkpoint to \code{\link[=zuk_models]{zuk_models()}} or make an arbitrary model ID
loadable through \code{\link[=zuk_pretrained]{zuk_pretrained()}}. The loader consults this registry only
after resolving a checkpoint from \code{zukzeit}'s package-owned curated catalogue.
Architecture authors can use \code{\link[=zuk_check_architecture]{zuk_check_architecture()}} independently;
catalogue inclusion requires the checkpoint's metadata, immutable revision,
licence, and numerical-parity evidence to be reviewed in \code{zukzeit}.
}
\examples{
zuk_registry_has("timesfm")
zuk_registry_archs()
}
