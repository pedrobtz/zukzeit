% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batching.R
\name{zuk_run_batches}
\alias{zuk_run_batches}
\title{Run a model over many series in batches}
\usage{
zuk_run_batches(
  model,
  contexts,
  horizons,
  quantile_levels,
  batch_size = NULL,
  device = NULL
)
}
\arguments{
\item{model}{A \code{zuk_model}.}

\item{contexts}{A list of numeric context vectors (oldest first).}

\item{horizons}{A list/vector of per-series integer horizons.}

\item{quantile_levels}{Numeric vector of quantile levels.}

\item{batch_size}{Series per batch for vectorised models; defaults to
\code{getOption("zuk.batch_size", 64L)}.}

\item{device}{Device string; resolved via \code{\link[=zuk_resolve_device]{zuk_resolve_device()}}.}
}
\value{
A list of quantile matrices.
}
\description{
Truncates each context to the model's \code{max_context}, then evaluates the
model. Returns a list, aligned to \code{contexts}, of \verb{h x length(quantile_levels)}
predictive-quantile matrices.
}
\examples{
model <- zuk_pretrained("stub")

# Two series of different lengths, each with its own horizon.
quantiles <- zuk_run_batches(
  model,
  contexts = list(cumsum(rep(1, 40)), cumsum(rep(2, 25))),
  horizons = c(3L, 5L),
  quantile_levels = c(0.1, 0.5, 0.9)
)
lengths(quantiles)
quantiles[[1]]

zuk_unload("stub")
}
