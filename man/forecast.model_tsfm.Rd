% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fable.R
\name{forecast.model_tsfm}
\alias{forecast.model_tsfm}
\title{Forecast from a composed tidyverts foundation model}
\usage{
\method{forecast}{model_tsfm}(object, new_data, specials = NULL, ...)
}
\arguments{
\item{object}{A fitted \code{model_tsfm}.}

\item{new_data}{Future rows supplied by \code{fabletools}.}

\item{specials}{Unused; supplied by \code{fabletools}.}

\item{...}{Unused.}
}
\value{
A \code{distributional} vector.
}
\description{
The \code{\link[generics:forecast]{generics::forecast()}} method for a \code{TSFM()} fit. Returns a
\code{distributional} vector of predictive quantiles, one element per row of
\code{new_data}, which is what \code{fabletools} assembles into a fable.
}
\examples{
\dontshow{if (requireNamespace("fabletools", quietly = TRUE) && requireNamespace("tsibble", quietly = TRUE)) withAutoprint(\{ # examplesIf}
history <- tsibble::tsibble(time = 1:24, value = cumsum(1:24), index = time)
fits <- fabletools::model(history, zukzeit = TSFM(value, model_id = "stub"))

# fabletools calls this method one key at a time; it returns the predictive
# distributions it then assembles into a fable.
fabletools::forecast(fits, h = 3)

zuk_unload("stub")
\dontshow{\}) # examplesIf}
}
