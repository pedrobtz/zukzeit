% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parsnip.R
\name{update.tsfm_reg}
\alias{update.tsfm_reg}
\title{Update a tsfm model specification}
\usage{
\method{update}{tsfm_reg}(object, parameters = NULL, context_length = NULL, fresh = FALSE, ...)
}
\arguments{
\item{object}{A \code{\link[=tsfm_reg]{tsfm_reg()}} specification.}

\item{parameters}{A one-row tibble or named list of parameter values, as
supplied by \code{tune::finalize_workflow()}.}

\item{context_length}{New context length, or \code{NULL} to leave it unchanged.}

\item{fresh}{If \code{TRUE}, replace the argument set rather than modifying it.}

\item{...}{Not used.}
}
\value{
An updated \code{\link[=tsfm_reg]{tsfm_reg()}} specification.
}
\description{
The \code{\link[stats:update]{stats::update()}} method every \code{parsnip} specification is expected to
provide. \code{tune} reaches it through \code{tune::finalize_workflow()} when it
substitutes a chosen \code{context_length} back into the spec, so without it the
whole tuning path fails with \code{update.default()}'s "need an object with call
component".
}
\examples{
\dontshow{if (requireNamespace("parsnip", quietly = TRUE)) withAutoprint(\{ # examplesIf}
spec <- tsfm_reg(context_length = 512L)
update(spec, context_length = 128L)
\dontshow{\}) # examplesIf}
}
