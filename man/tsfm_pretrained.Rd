% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hub.R
\name{tsfm_pretrained}
\alias{tsfm_pretrained}
\title{Load a time-series foundation model from the Hugging Face Hub}
\usage{
tsfm_pretrained(model_id, revision = NULL, device = NULL, reuse = TRUE, ...)
}
\arguments{
\item{model_id}{Character scalar. A Hub repo id such as
\code{"ibm-granite/granite-timeseries-ttm-r2"}, or \code{"stub"} for the built-in
Stage 0 model.}

\item{revision}{Character scalar. A branch, tag, or commit SHA to pin.
\code{NULL} uses the immutable revision in the catalogue for curated models and
\code{"main"} for the built-in stub.}

\item{device}{Device requested for model construction; resolved through
\code{\link[=tsfm_resolve_device]{tsfm_resolve_device()}}.}

\item{reuse}{Logical; reuse an identical constructed handle from the bounded
R-session cache. \code{FALSE} constructs a fresh handle without deleting files.}

\item{...}{Load-affecting architecture options included in the resident
cache key and made available through \code{config$load_options}.}
}
\value{
A \code{\link[=new_tsfm_model]{new_tsfm_model()}}.
}
\description{
Resolves a model's configuration, looks up the matching architecture
constructor in the registry, and returns a uniform \code{\link[=new_tsfm_model]{new_tsfm_model()}} handle.
}
\details{
The built-in \code{"stub"} model is fully wired: passing
\code{"stub"} (or any id beginning with \code{"stub"}) synthesises a config in memory
and skips download and torch. For a supported real checkpoint the function
resolves its immutable catalogue revision, downloads the required manifest,
validates and loads the safetensors state dict, dispatches to the registered
constructor, and optionally retains the constructed handle in a bounded
R-session LRU. Scaffold and experimental checkpoints fail before download;
use \code{\link[=tsfm_download]{tsfm_download()}} only when explicitly prefetching their files.
}
\examples{
model <- tsfm_pretrained("stub")
tsfm_capabilities(model)
tsfm_unload("stub")
}
