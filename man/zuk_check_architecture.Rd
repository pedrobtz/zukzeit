% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conformance.R
\name{zuk_check_architecture}
\alias{zuk_check_architecture}
\title{Verify an architecture against the zukzeit contract}
\usage{
zuk_check_architecture(
  constructor,
  config = list(),
  weights = NULL,
  context = default_check_context(),
  h = 6L,
  quantile_levels = c(0.1, 0.5, 0.9),
  tolerance = 1e-08,
  rtol = 1e-05,
  crossing_tolerance = tolerance,
  error = !interactive()
)
}
\arguments{
\item{constructor}{A function \verb{(config, weights)} returning a
\code{\link[=new_zuk_model]{new_zuk_model()}}, as passed to \code{\link[=zuk_register_arch]{zuk_register_arch()}}.}

\item{config}{A config list handed to \code{constructor}. Supply whatever the
architecture needs; the default is empty.}

\item{weights}{Weights handed to \code{constructor}. Default \code{NULL}.}

\item{context}{Numeric context vector used for the probe forecasts. Defaults
to a deterministic trend-plus-seasonality series of length 64.}

\item{h}{Probe horizon.}

\item{quantile_levels}{Probe quantile levels.}

\item{tolerance}{Absolute tolerance for the batch-agreement check.}

\item{rtol}{Relative tolerance for the batch-agreement check. Agreement is
asserted as \verb{|batch - loop| <= tolerance + rtol * |loop|}, the criterion
the parity fixtures use. An absolute threshold alone cannot work here: a
batched matrix multiply is not obliged to reduce in the same order as an
unbatched one, so float32 kernels differ by a few ulps of the \emph{values},
and a checkpoint forecasting in the hundreds cannot meet a fixed 1e-8.}

\item{crossing_tolerance}{How far quantiles may cross before the
monotonicity check fails. Defaults to \code{tolerance}.}

\item{error}{If \code{TRUE} (the default when called non-interactively), fail on
the first violated invariant. If \code{FALSE}, return the full report so every
problem is visible at once.}
}
\value{
Invisibly, a data frame with one row per check: \code{name}, \code{ok}
(\code{TRUE}, \code{FALSE}, or \code{NA} for not applicable), and \code{message}.
}
\description{
Runs the invariants in \verb{?}\link{zuk-architecture-contract} against an
architecture constructor and reports which hold. Use it while implementing a
new architecture, and in that architecture's test suite as a regression gate.
}
\details{
This checks \emph{contract conformance}, not \emph{numerical correctness}. An
architecture can pass every check here and still forecast badly; matching the
reference implementation is a separate gate, covered by golden parity
fixtures (see \code{tests/testthat/fixtures/README.md}).
}
\examples{
# A minimal conforming architecture: last value, with a Gaussian spread.
demo_arch <- function(config, weights) {
  new_zuk_model(
    architecture = "demo",
    config = config,
    capabilities = new_zuk_capabilities("demo", max_context = 128L),
    predict_fn = function(context, h, quantile_levels) {
      if (length(context) == 0L) stop("empty context")
      last <- context[length(context)]
      outer(rep(last, h), stats::qnorm(quantile_levels), `+`)
    }
  )
}

report <- zuk_check_architecture(demo_arch, error = FALSE)
report
}
\seealso{
\verb{?}\link{zuk-architecture-contract}
}
