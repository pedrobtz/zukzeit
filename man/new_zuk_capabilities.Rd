% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/capabilities.R
\name{new_zuk_capabilities}
\alias{new_zuk_capabilities}
\title{Capability metadata for a time-series foundation model}
\usage{
new_zuk_capabilities(
  architecture,
  max_context,
  max_horizon = Inf,
  quantiles = c("native", "none"),
  quantile_levels = NULL,
  multivariate = FALSE,
  samples = FALSE,
  past_covariates = FALSE,
  future_covariates = FALSE,
  static_covariates = FALSE,
  fine_tunable = FALSE,
  license = NA_character_
)
}
\arguments{
\item{architecture}{Character scalar, the architecture key (e.g. \code{"ttm"}).}

\item{max_context}{Integer, the maximum supported context length.}

\item{max_horizon}{Numeric scalar, the maximum supported forecast horizon,
or \code{Inf} when the architecture has no smaller declared limit.}

\item{quantiles}{One of \code{"native"} (the model emits quantiles directly) or
\code{"none"}.}

\item{quantile_levels}{Numeric vector of quantile levels the checkpoint can
emit, or \code{NULL} when a native-quantile architecture accepts arbitrary
levels.}

\item{multivariate, samples, past_covariates, future_covariates, static_covariates, fine_tunable}{Reserved logical fields. They must remain \code{FALSE} in contract
v1 because the forward signature has no channel for those inputs or
outputs.}

\item{license}{Character scalar, the SPDX identifier of the \emph{weight} licence.}
}
\value{
A \code{zuk_capabilities} object.
}
\description{
A \code{zuk_capabilities} object is a small record describing what a model can
and cannot do. It is attached to every model returned by
\code{\link[=zuk_pretrained]{zuk_pretrained()}} and is used for \emph{pre-flight validation}: a fit or
forecast call is rejected with an informative error \strong{before} any tensor
work if the request exceeds the model's declared capabilities.
}
\examples{
new_zuk_capabilities("demo", max_context = 512L)

# A checkpoint with fixed trained quantile levels declares them, and the
# engine then refuses any other level before running the forward pass.
new_zuk_capabilities(
  "demo",
  max_context = 512L,
  max_horizon = 64,
  quantile_levels = c(0.1, 0.5, 0.9),
  license = "Apache-2.0"
)
}
