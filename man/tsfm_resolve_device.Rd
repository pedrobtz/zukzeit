% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batching.R
\name{tsfm_resolve_device}
\alias{tsfm_resolve_device}
\title{Resolve a compute device}
\usage{
tsfm_resolve_device(device = NULL)
}
\arguments{
\item{device}{One of \code{"auto"}, \code{"cpu"}, \code{"mps"}, \code{"cuda"}, \code{"cuda:N"}, or
\code{NULL} to read the \code{tsfm.device} option.}
}
\value{
A concrete device string (never \code{"auto"}).
}
\description{
Returns a concrete device string for torch models. \code{"auto"} (the default,
overridable with \code{options(tsfm.device = ...)}) picks CUDA, then MPS, then CPU,
based on what the installed torch backend reports, and warns if a requested
accelerator is unavailable, falling back to CPU. Without torch it is always
\code{"cpu"}, so the stub path never touches torch.
}
\examples{
tsfm_resolve_device("cpu")

# "auto" picks the best backend the installed torch reports.
tsfm_resolve_device("auto")
}
