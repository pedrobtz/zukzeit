% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contract.R
\name{tsfm-architecture-contract}
\alias{tsfm-architecture-contract}
\title{The tsfm architecture contract}
\description{
\code{tsfm} is an inference engine: it owns the path from a pretrained checkpoint
to predictive quantiles, and nothing above it. An \emph{architecture} is a
constructor and forward pass satisfying the contract described here.
}
\details{
\code{\link[=tsfm_register_arch]{tsfm_register_arch()}} associates a constructor with an architecture key in
the current R session. Registration does not add checkpoint metadata to the
package-owned catalogue and does not make arbitrary model IDs available to
\code{\link[=tsfm_pretrained]{tsfm_pretrained()}} or model-ID-based adapters. A checkpoint is exposed by
those APIs only after it is curated in \code{tsfm}, including an immutable
revision, manifest, licence metadata, contract conformance, and numerical
parity. Independently constructed \code{\link[=new_tsfm_model]{new_tsfm_model()}} handles can still use
the framework-neutral \code{\link[=forecast]{forecast()}} path.

Verify an implementation with \code{\link[=tsfm_check_architecture]{tsfm_check_architecture()}}.
}
\section{The constructor}{

A constructor is a function of \verb{(config, weights)} returning a
\code{\link[=new_tsfm_model]{new_tsfm_model()}}:
\itemize{
\item \code{config} — a named list parsed from the checkpoint's \code{config.json}, with
\code{architecture}, \code{model_id}, and \code{revision} filled in by the loader.
\item \code{weights} — a state dict, or \code{NULL} for weight-free architectures.
}

The constructor must not perform inference, and must not download anything
beyond what the loader already resolved.
}

\section{The forward pass}{

Every architecture supplies \code{predict_fn}, and may additionally supply
\code{predict_batch_fn} for true vectorised inference.

\if{html}{\out{<div class="sourceCode">}}\preformatted{predict_fn(context, h, quantile_levels) -> matrix[h, q]
}\if{html}{\out{</div>}}
\itemize{
\item \code{context} — a numeric vector of observed history, \strong{oldest first}, already
truncated to \code{max_context} and with \code{NA} removed by \code{\link[=tsfm_run_batches]{tsfm_run_batches()}}.
\item \code{h} — a positive integer horizon.
\item \code{quantile_levels} — a sorted numeric vector in \verb{(0, 1)}.
\item Returns a numeric matrix with \code{h} rows and \code{length(quantile_levels)}
columns. Column \code{j} holds the forecasts at \code{quantile_levels[j]}.
}

\if{html}{\out{<div class="sourceCode">}}\preformatted{predict_batch_fn(contexts, horizons, quantile_levels, device) -> list(matrix)
}\if{html}{\out{</div>}}
\itemize{
\item \code{contexts} — a list of numeric context vectors.
\item \code{horizons} — an integer vector of per-series horizons, aligned to
\code{contexts}.
\item \code{device} — a resolved device string (never \code{"auto"}); see
\code{\link[=tsfm_resolve_device]{tsfm_resolve_device()}}.
\item Returns a list of quantile matrices, aligned to and the same length as
\code{contexts}.
}

When \code{predict_batch_fn} is \code{NULL}, \code{\link[=tsfm_run_batches]{tsfm_run_batches()}} loops \code{predict_fn}.
When both are present they must agree: the batch path is an optimisation,
never a different model.
}

\section{Invariants}{

These hold for every architecture and are what \code{\link[=tsfm_check_architecture]{tsfm_check_architecture()}}
asserts:
\itemize{
\item \strong{Shape.} The result is a numeric matrix, \code{h} by \code{length(quantile_levels)}.
\item \strong{Monotonicity.} Within a row, forecasts are non-decreasing across
quantile levels. A model whose raw output can cross must sort before
returning.
\item \strong{Finiteness.} No \code{NA}, \code{NaN}, or \code{Inf} for a finite, non-empty context.
\item \strong{Context limit.} The model never requires more history than the
\code{max_context} it declares in its \code{\link[=new_tsfm_capabilities]{new_tsfm_capabilities()}}.
\item \strong{Empty context.} An empty context raises an error rather than returning
\code{NA} — silent nonsense is worse than a stop.
\item \strong{Batch agreement.} If \code{predict_batch_fn} is supplied, it matches
\code{predict_fn} within numerical tolerance.
}
}

\section{Capabilities}{

Capabilities are declarations the engine enforces \emph{before} inference, so an
unsupported request costs nothing. Declaring a capability the forward pass
does not honour is a bug in the architecture. Contract v1 has channels only
for univariate numeric context and predictive quantiles: multivariate,
covariate, sample-path, and fine-tuning declarations must remain \code{FALSE}.
Native-quantile checkpoints declare their exact trained levels when those
levels are fixed, and the engine validates them together with horizon and
context limits before calling the architecture. See
\code{\link[=new_tsfm_capabilities]{new_tsfm_capabilities()}}.
}

\section{Versioning}{

\code{\link[=new_tsfm_model]{new_tsfm_model()}} stamps every handle with \code{\link[=tsfm_contract_version]{tsfm_contract_version()}}. The
contract follows semantic versioning: the major component changes only for a
breaking change to the signatures or invariants above, and never without a
deprecation cycle. Architectures may record the version they were written
against by passing \code{contract_version} explicitly.
}

\seealso{
\code{\link[=tsfm_register_arch]{tsfm_register_arch()}}, \code{\link[=new_tsfm_model]{new_tsfm_model()}},
\code{\link[=tsfm_check_architecture]{tsfm_check_architecture()}}, \code{\link[=new_tsfm_capabilities]{new_tsfm_capabilities()}}
}
