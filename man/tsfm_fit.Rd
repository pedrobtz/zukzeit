% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{tsfm_fit}
\alias{tsfm_fit}
\alias{tsfm_fit.formula}
\alias{tsfm_fit.data.frame}
\title{Fit (bind) a foundation model to training history}
\usage{
tsfm_fit(x, ...)

\method{tsfm_fit}{formula}(
  x,
  data,
  model,
  index,
  id = NULL,
  quantile_levels = c(0.1, 0.5, 0.9),
  ...
)

\method{tsfm_fit}{data.frame}(
  x,
  model,
  target,
  index,
  id = NULL,
  quantile_levels = c(0.1, 0.5, 0.9),
  ...
)
}
\arguments{
\item{x}{A \code{formula} or a \code{data.frame}.}

\item{...}{Passed to methods.}

\item{data}{Training data: one row per (series, time) with the target,
a time \code{index} column, and an optional series \code{id} column.}

\item{model}{A \code{tsfm_model} from \code{\link[=tsfm_pretrained]{tsfm_pretrained()}}.}

\item{index, id}{Column names of the time index and (optional) series id.}

\item{quantile_levels}{Numeric vector of quantile levels to forecast.}

\item{target}{Column name of the target series (data.frame method).}
}
\value{
A \code{tsfm_fit} object.
}
\description{
Fit (bind) a foundation model to training history
}
\examples{
model <- tsfm_pretrained("stub")
train <- data.frame(
  store = rep(c("a", "b"), each = 40),
  day   = rep(1:40, 2),
  sales = c(cumsum(rep(2, 40)) + 100, cumsum(rep(1, 40)) + 50)
)

# Formula interface: the left-hand side names the target series.
fit <- tsfm_fit(sales ~ ., data = train, model = model,
                index = "day", id = "store")
fit

# Or name the columns directly.
tsfm_fit(train, model = model, target = "sales", index = "day", id = "store")

tsfm_unload("stub")
}
